apiVersion: v1
kind: Service
metadata:
  name: consensus-layer-svc
spec:
  type: ClusterIP
  selector:
    app: consensus-layer
  ports:
    - name: rpc
      port: 5052
      protocol: TCP
      targetPort: rpc
    - name: http
      port: 5053
      protocol: TCP
      targetPort: http
    - name: metrics
      port: 5054
      protocol: TCP
      targetPort: metrics
    - name: peeringTcp
      port: 9000
      protocol: TCP
      targetPort: peeringtcp
    - name: peeringUdp
      port: 9000
      protocol: UDP
      targetPort: peeringudp
    - name: peering
      port: 9001
      protocol: UDP
      targetPort: peering
